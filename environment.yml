name: investment_planning
channels:
  - conda-forge
  - defaults
dependencies:
  # Python version
  - python=3.9

  # Core optimization
  - numpy>=1.21.0
  - scipy>=1.7.0

  # Data manipulation
  - pandas>=1.3.0

  # Visualization
  - matplotlib>=3.4.0
  - seaborn>=0.11.0
  - plotly>=5.0.0

  # Jupyter
  - jupyter>=1.0.0
  - notebook>=6.4.0
  - ipywidgets>=7.6.0

  # R and R packages
  - r-base>=4.0
  - r-lpsolve
  - r-ggplot2
  - r-dplyr
  - r-tidyr
  - r-knitr
  - r-rmarkdown

  # Testing
  - pytest>=7.0.0
  - pytest-cov>=3.0.0

  # Pip packages
  - pip
  - pip:
    - PuLP>=2.7.0
    - black>=22.0.0
    - flake8>=4.0.0
    - sphinx>=4.3.0
    - sphinx-rtd-theme>=1.0.0
